import {Text} from 'react-native';
import React from 'react';
import {Container} from '../../../components/';

const GroupsScreen: React.FC = props => {
  return (
    <Container>
      <Text style={{color: 'red'}}>GroupsScreen</Text>
    </Container>
  );
};

export default GroupsScreen;
